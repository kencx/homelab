datacenter   = "dc1"
data_dir     = "/opt/nomad/data"
bind_addr = "0.0.0.0"

client {
  enabled = true
  servers = ["{{ nomad_server_ip }}"]
}

plugin "docker" {
  config {
    volumes {
      enabled = true
    }
  }
}
