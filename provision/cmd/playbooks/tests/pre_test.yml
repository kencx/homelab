---
- hosts: cmd, drone
  vars_files:
    - ../vars.yml
  tasks:
    - set_fact:
        nfs_mounts: "{{ ansible_mounts | map(attribute='mount') | list }}"

    - name: Check if mounts exist
      debug:
        var: nfs_mounts
      failed_when: docker_destination not in nfs_mounts

    # - name: Mount bind mount
    #   become: true
    #   mount:
    #     src: "10.10.10.102:/home/debian/apps/cmd-apps"
    #     path: "{{ docker_destination }}"
    #     fstype: nfs
    #     state: present
